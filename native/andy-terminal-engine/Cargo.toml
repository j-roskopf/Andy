[package]
name = "andy_terminal_engine"
version = "0.1.0"
edition = "2021"
description = "Spike: alacritty_terminal engine for Andy, exposed to the JVM via JNI"
license = "MIT"
publish = false

[lib]
name = "andy_terminal_engine"
crate-type = ["cdylib", "rlib"]

[dependencies]
alacritty_terminal = "0.26"
parking_lot = "0.12"
# jni / uniffi added in a later commit once the engine API is proven.

[dev-dependencies]
# in-crate tests only

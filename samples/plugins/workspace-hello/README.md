# Workspace Hello

Minimal sample that prints `ANDY_PLUGIN_CONTEXT_JSON` and records startups.

```sh
andy plugin link samples/plugins/workspace-hello
andy plugin action invoke print-context --plugin examples.workspace-hello
```

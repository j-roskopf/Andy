# Pane Echo

Opens plugin-owned Terminal dock tabs.

```sh
andy plugin link samples/plugins/pane-echo
andy plugin pane open --plugin examples.pane-echo --entrypoint echo-tab
andy plugin pane open --plugin examples.pane-echo --entrypoint echo-split --placement split
```
